name: "Docker"

on: [push]

jobs:
  compile-and-push:
    name: "Compile And Push"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker
        uses: docker/setup-buildx-action@v2
      -
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and Push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: "zencommunity/core:${{ github.sha }}"
          context: .
          file: Dockerfile
          no-cache: true
      -
        name: Digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  execute:
    name: "Run"
    runs-on: ubuntu-latest
    container: "zencommunity/core:${{ github.sha }}"
    needs: [compile-and-push]
    steps:
      -
        name: Run
        run: cd /app && ./Core
      -
        name: Digest
        run: echo "${{ github.sha }}"