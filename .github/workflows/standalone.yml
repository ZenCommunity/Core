name: "Standalone"

on: [push]

jobs:
  execute:
    name: "Build, Test and Run"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environments: [Debug, Release]
    steps:
      - uses: actions/checkout@v3
      -
        name: Dependencies
        run: |
          sudo apt update 
          sudo apt install build-essential libgmock-dev
          sudo wget -q https://boostorg.jfrog.io/artifactory/main/release/1.80.0/source/boost_1_80_0.tar.gz
          sudo tar -xf boost_1_80_0.tar.gz
          cd boost_1_80_0
          sudo ./bootstrap.sh
          sudo ./b2 install
          cd ..
      -
        name: Build, Test and Run
        env:
          APP_ENV: ${{ matrix.environments }}
          APP_PORT: 5000
        run: |
          cd source
          sudo cmake -S . -B . \
            -DCMAKE_BUILD_TYPE=${{ matrix.environments }}
          sudo cmake --install . --strip
          sudo cmake --build . --config ${{ matrix.environments }}
          ./Test
          ./Core
      -
        name: Digest
        run: echo "${{ github.sha }}"
      -
        name: Zip Binary
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: montudor/action-zip@v1
        with:
          args: zip -qq -r ${{ github.ref_name }}-${{ matrix.environments }}.zip source/Core
      - name: Upload Binary
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GH_API_TOKEN }}
          file: ${{ github.ref_name }}-${{ matrix.environments }}.zip
          asset_name: "${{ github.ref_name }}—${{ matrix.environments }}.zip"
          tag: ${{ github.ref }}
          overwrite: true
